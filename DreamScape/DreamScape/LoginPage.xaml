<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DreamScape.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DreamScape"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">

            <!-- Username Field -->
            <TextBox x:Name="UsernameTextBox" Width="300" PlaceholderText="Username" />

            <!-- Password Field -->
            <PasswordBox x:Name="PasswordBox" Width="300" PlaceholderText="Password" />

            <!-- Login Button -->
            <Button Content="Login" Width="300" Click="LoginButton_Click" />

            <!-- Error Message (initially hidden) -->
            <TextBlock x:Name="ErrorMessage" Foreground="Red" Visibility="Collapsed" Text="Invalid username or password" />
        </StackPanel>
    </Grid>
</Page>
